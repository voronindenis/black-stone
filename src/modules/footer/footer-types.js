// @flow
import * as React from 'react';

export type LinksRowsItemsType = {
  title: string,
  id: string,
  link: string,
};

export type IconInfoBlockItemsType = {
  id: string,
  title: string,
  text: React.Element<*>,
  icon: string,
};
